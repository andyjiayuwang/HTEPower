% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hte_power.R
\name{hte_power}
\alias{hte_power}
\title{Solve the simplified HTE power formula}
\usage{
hte_power(
  n = NULL,
  alpha = NULL,
  kappa = NULL,
  p = NULL,
  sigma2 = NULL,
  var_s0 = NULL
)
}
\arguments{
\item{n}{Total sample size. Must be a positive integer when supplied.}

\item{alpha}{Two-sided significance level in \verb{(0, 1)}.}

\item{kappa}{Target power in \verb{[0.5, 1)}.}

\item{p}{Treatment probability in \verb{(0, 1)}.}

\item{sigma2}{Outcome disturbance variance, which must be positive.}

\item{var_s0}{Variance of the CATE, \verb{Var\{s0(X)\}}, which must be positive.}
}
\value{
A numeric equality boundary for the missing quantity. For an unknown
\code{n}, the boundary is rounded up and returned as an integer. For an unknown
\code{p}, one or two feasible treatment probabilities are returned.
}
\description{
Solve for exactly one missing quantity in the simplified power formula
\deqn{n = \frac{\sigma^2(z_{1-\alpha/2}+z_\kappa)^2}
{p(1-p)\mathrm{Var}\{s_0(X)\}}.}
All supplied quantities must be numeric scalars. Set exactly one argument to
\code{NULL}; the function returns the equality boundary implied by the other five
arguments. When \code{n} is unknown, the result is rounded up to the next integer.
}
\details{
Because \code{p * (1 - p)} is symmetric around \code{p = 0.5}, solving for \code{p} can
produce two equivalent treatment probabilities. In that case, both are
returned as a named numeric vector with elements \code{lower} and \code{upper}.
}
\examples{
hte_power(
  n = NULL, alpha = 0.05, kappa = 0.8, p = 0.5,
  sigma2 = 10, var_s0 = 0.25
)

hte_power(
  n = 1256, alpha = 0.05, kappa = 0.8, p = NULL,
  sigma2 = 10, var_s0 = 0.25
)
}
